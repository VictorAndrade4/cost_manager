<%= @item.name %>
<br>
<%= "Price: #{@item.price}" %>
<br>


<strong>Category:</strong>
<%= @item.category.name %>

<br>
<%= link_to 'Destroy', @item, method: :delete, data: { confirm: 'Are you sure?' } %>
